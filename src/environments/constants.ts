import { menu_links } from '../app/system/menu.links';

export const constants = {
  secretKey: 'saipci key',
  appName: 'SAIPCI',
  loginURL: '/api/v1/authenticate',
  refreshTokenURL: '/api/v1/refreshToken',
  authenticatedURL: '/api/v1/users/authenticated',
  menuLinks: menu_links,
  clientId: 'ngAuthApp',
};
